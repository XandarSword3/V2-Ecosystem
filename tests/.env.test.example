# e2e/.env.test
# Copy this to e2e/.env.test and fill in values. Never commit the real file.
# This file is the template — it's safe to commit.

# Supabase — point at the DEV project for now (no separate test project yet)
# Get from: backend/.env.test
SUPABASE_URL=https://qxtmesddgwmwspejnbvc.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=

# Stripe — test mode keys only, never production
STRIPE_SECRET_KEY=sk_test_
STRIPE_WEBHOOK_SECRET=whsec_
STRIPE_PUBLISHABLE_KEY=pk_test_

# Test user credentials — must exist in the dev DB
# Create via: POST /api/v1/auth/register on testcorp.localhost:3000
E2E_ADMIN_EMAIL=admin@testcorp.v2platform.com
E2E_ADMIN_PASSWORD=
