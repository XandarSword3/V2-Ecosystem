# Frontend Types

TypeScript type definitions specific to the frontend.

## Purpose

Contains frontend-specific types for:
- Component props
- API response shapes
- Form data interfaces
- UI state types

For shared types used by both frontend and backend, see `shared/types/`.
