# Module Builder Components

Dynamic module configuration UI that allows admins to enable, disable, and customize resort modules at runtime.

## Purpose

Provides a visual interface for the module management system, allowing non-technical users to:
- Toggle modules on/off
- Configure module-specific settings
- Set up custom fields and options per module
- Preview module configurations
